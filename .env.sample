# Django settings
SECRET_KEY=change-me-in-production
DEBUG=true
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (default SQLite). To use Postgres set ENGINE and vars below.
DB_ENGINE=sqlite
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
